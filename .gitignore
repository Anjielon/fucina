__pycache__/
*.pyc
forge-work/
*.log
*.blg
*.gguf
*.npy

# Proposed GitHub description/topics — a note to the maintainer, not content.
.github_meta.md

# ⛔ SAFETY NET — unreviewed internal notes.
# These are working documents in Italian that still contain local host names,
# private IP addresses and paths from the deployment environment. They are
# ignored so that a `git add -A` cannot publish them by accident.
# Delete the matching line here once the file has been translated and scrubbed.
docs/NOTTE_2026_08_28.md
docs/RICETTA_TONY_PERFETTA.md
